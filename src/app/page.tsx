import { Navbar, HomeSection, AboutSection, WorkSection, EducationSection, FooterSection } from "../components";

export default function Page() {
  return (
    <main>
      <Navbar />
      <HomeSection />
      <AboutSection />
      <WorkSection />
      <EducationSection />
      <FooterSection />
    </main>
  );
}
